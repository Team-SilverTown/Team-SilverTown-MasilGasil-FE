import { FONT_SIZE, FONT_WEIGHT } from "@/styles/theme";
import styled from "styled-components";

export const MapLocationModalDetail = styled.p`
  width: 100%;
  max-width: 26rem;
  margin-bottom: 2rem;

  text-align: center;

  font-size: ${FONT_SIZE.BASIC};
  font-weight: ${FONT_WEIGHT.SEMIBOLD};
`;
